# handoff-11 (side catalog)

Alternate **11-cube** fold paths. Not the iMessage / Linq default.

The live booth library is `handoff-17/`. To use these instead:

```bash
export CUBOT_HANDOFF_DIR="$(pwd)/handoff-11"
export CUBOT_N_MODULES=11
export CUBOT_SCENE_XML="../cubot_urdf/n11/scene.xml"
```
